# Memora GitHub Actions Configuration
# TEMPORARY: Disabled until Xcode project is pushed and ready
name: iOS CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

# TEMPORARY: Disabled until Xcode project is ready
# Uncomment this section when ready to re-enable CI
