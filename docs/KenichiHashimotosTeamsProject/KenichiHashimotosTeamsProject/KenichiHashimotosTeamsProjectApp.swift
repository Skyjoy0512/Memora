import SwiftUI

/// Main entry point for the Files application.
/// Displays the FilesScreen as the root view.
@main
struct FilesApp: App {
    var body: some Scene {
        WindowGroup {
            FilesScreen()
        }
    }
}
